import React from "react";
import Layout from "@components/Layout";

const Post = (props) => {
  // const { title, html } = props?.data?.allOrgFile;
  console.log({ props });
  return (
    <Layout>
      Projects page WIP
      {/* <h1>{title}</h1> */}
      {/* <section dangerouslySetInnerHTML={{ __html: html }} /> */}
    </Layout>
  );
};

export default Post;
